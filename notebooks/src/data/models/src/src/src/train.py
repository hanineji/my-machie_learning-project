from data_loader import load_csv
from model import build_mlp

# Esempio d'uso (placeholder)
if __name__ == "__main__":
    # df = load_csv("data/dataset.csv")
    # model = build_mlp(input_dim=df.shape[1]-1)
    # model.fit(...)
    pass
